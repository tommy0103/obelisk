# Codex fixtures

`streaming-rollout.jsonl` is a privacy-sanitized excerpt of Codex CLI rollout
output observed on 2026-06-10. Identifiers, paths, text, and token counts were
replaced; envelope types, field layout, payload shapes, and event ordering were
retained for provider compatibility tests.
